# Layout Component 設計書

## 1. 概要
アプリケーション全体の共通レイアウト（ヘッダー、フッターなど）を提供するラッパーコンポーネント。

## 2. 構造
・ `<Header />`コンポーネントを上部に表示する。
・ `{children}` propを受け取り、ヘッダーの下にメインコンテンツとして表示する。
・ (将来的に) `<Footer />`コンポーネントを下部に表示する。

## 3. Props
・ `children` (ReactNode): `Layout`コンポーネントでラップされるページコンテンツ。
